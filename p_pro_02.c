#include "holberton.h"

/
/**
 * p_r - print reversed string
 * @p: argument pointer
 * Return: length of the output sent to the output
 */
int p_r(va_list p)
{
	int len2 = 0;
	char *s = va_arg(p, char*);
if (*s)
{
	int p_r(va_list p + 1);
	len2 += _putchar(*s);
}
	return (len2 = 0);
}