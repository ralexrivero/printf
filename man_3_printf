.\" Manpage for _printf
.\" Contact 2787@holbertonschool.com to correct errors or typos.
.TH _printf (3) "7/24/2021" "5.0" "_printf Programmer's Manual"
.SH NAME
_printf 
.SH SYNOPSIS
/**
 * _printf - print
 * @format: cadena
 * Return: dad
*/
#include "holberton.h"

int _printf(const char *format, ...)
.SH DESCRIPTION
Produce output according to a format.
Write output to stdout, the standard output stream.
Returns the number of characters printed.
All of these functions write the output under the control of a
format string that specifies how subsequent arguments (or
arguments accessed via the variable-length argument facilities
are converted for output.
Format of the format string:
The format string is a character string, beginning and ending in
its initial shift state, if any.  The format string is composed
of zero or more directives: ordinary characters (not %), which
are copied unchanged to the output stream; and conversion
specifications, each of which results in fetching zero or more
subsequent arguments.  Each conversion specification is
introduced by the character %, and ends with a conversion
specifier.  In between there may be (in this order) zero or more
flags, an optional minimum field width, an optional precision and
an optional length modifier.
.SH OPTIONS
.B %c -  prints a single char
.B %s -  prints the argument as a string
.B %% -  prints a single perecentage sign to the stream
.B %i / %d - prints signed decimal integer
.SH EXAMPLES
/**
 * _putchar - print char
 * @c: argument pointer
 * Return: write(1, &c, 1
 */
#include <unistd.h>
int _putchar(char c)
{
        return (write(1, &c, 1));
}
/**
 * p_s - print string
 * @p: argument pointer
 * Return: 1
 */
int p_s(va_list p)
{
	int i = 0;
	char *s;

	s = va_arg(p, char*);
	while(s[i] != '\0');
	{
		_putchar(s[i]);
		i++;
	}
	return(i);
}

.SH BUGS
No known bugs.
.SH AUTHORS
Diego G, Ronald R, Joaquin P
.SH COPYRIGHT
    Holberton Uruguay Cohorte 15.
